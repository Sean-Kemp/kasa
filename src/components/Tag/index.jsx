import React from 'react';

function Tag({ tag }) {
      return (
            <div className="tag">
                  <span className="tag__text">{tag}</span>
            </div>
      );
}

export default Tag;
